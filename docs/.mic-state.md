# Mic State — drop-note Working Memory

> Mic's persistent working memory across sessions. Updated at every checkout.
> Read FIRST on every session resume per `~/.claude/agents/drop-mic.md` § Session Resume Protocol.

---

## Last session

**2026-05-27 (Sessions 14–15 — CHECKOUT)** — Resend domain 修复确认 + Task F 完成 + PR #3 OPEN，覆盖率门槛失败待拍板。

**Session 15（晚）— Task F 收尾：**
- Jose 派遣 Task F（`s8/onboarding` 分支），inline 执行模式
- Jose 中途断了一次：sub-task 1 已 commit (`113fec8`)，sub-task 2 文件写了未 commit，resume 后接着干
- Task F 4 个 commits 全部完成：
  - `113fec8` `[s8] fix: remove silent error swallowing in email lib`
  - `d68d4f9` `[s8] feat: add WelcomeModal shown once per browser on first visit`
  - `762c2e7` `[s8] feat: upgrade empty state with 3-step pills and drop address CTA`
  - `8a69301` `[s8] docs: task-f verification screenshots + execution plan`
- 143 tests 全绿，lint/typecheck/build 全绿
- **Jose 主动 block 而非伪造证据** — Google OAuth only 无法做 headless 自动化，Jose 停下来 ping Mic 而不是跑假截图。新流程第一次落地的证据，记录为典范行为（见 Process Rules 章节）
- Jason 本机 manual verify：起 dev server（stale `.next` cache 导致 `__webpack_modules__` error，wipe `.next` + `.turbo` 后干净），用 `jasongaoxiaofeng@gmail.com` 次账号登录，截图存 `docs/verification/task-f/welcome-modal.png` + `empty-state.png`
- **PR #3 OPEN** — https://github.com/lego651/drop-note/pull/3

**PR #3 CI 状态（截至 2026-05-27 EOD）：**

| Check | 状态 |
|---|---|
| Lint | ✅ pass |
| Typecheck | ✅ pass |
| Unit tests + coverage | ❌ FAIL — statements 55.67% < 60%，branches 44.8% < 50%。143 tests 全过，Task F 新代码（WelcomeModal + empty state）覆盖度不够拉到门槛 |
| E2E tests | SKIPPED（gated on unit pass）|
| Vercel Preview | ✅ pass |

**明天第一件事：拍板覆盖率路径 (a/b/c)**

**Session 14（下午）— Task E 质量审计 + Resend domain 修复：**
- Task E 审计完成（详见 Process Rules）— TDD 缺失、verification 缺失导致 Resend 403 被吞
- **Resend domain `dropnote.me` 修复** — 用 Resend API 正式添加域名
- Jason 在 Porkbun 手动加 3 条 DNS：
  - TXT `resend._domainkey.dropnote.me` → DKIM key
  - MX `send.dropnote.me` → `feedback-smtp.us-east-1.amazonses.com`
  - TXT `send.dropnote.me` → SPF record
  - 状态：Verified
- **真邮件送达确认** — Jason 收到 2 封：直接测试 + 真 digest（"Your drop-note week — 1 save this week"）
- 3 条 process rules LOCKED（已写入 Process Rules 章节）
- Task F brief 已写好并 dispatch

**2026-05-27 (Session 13 — CHECKOUT)** — Task E 完成收尾 + 质量审计 + Task F brief 写好并 dispatch。

详情：
- PR #2 admin-merge → main (`36e228e`)
- Supabase migration 通过 Management API + PAT 跑通（CLI 无法拿 DB 密码）
- Types regen via `npx supabase gen types --project-id` + PAT
- CRON_SECRET 新生成 64-char hex，加到 Production + Preview + Development scope
- Prod redeploy 触发让 CRON_SECRET 生效
- Vercel Cron 2/2 注册：`/api/cron/purge-trash`（每天 3am UTC）+ `/api/cron/digest`（周一 9am UTC）
- **关键发现：Resend domain dropnote.me 之前从未在 Resend 侧添加。** 今天用 API 加了，Jason 手动加了 3 条 DNS（DKIM TXT + send.MX + send.SPF），现在 verified。
- Jason 收到 2 封邮件确认（1 直接测试 + 1 真 digest "Your drop-note week — 1 save this week"）
- Jason PAT 已 revoke

质量审计结论（Jose 的 Task E PR #2 不符合 superpowers 规范）：
- TDD: FAIL — PR #2 零个新测试文件，code-first 非 test-first
- verification-before-completion: FAIL — 声称 lint/typecheck/build green 就完事，未验证邮件送达
- 直接后果：`lib/email.ts:69-79` 的 `try/catch` 吞掉 Resend 403，cron 返回 `processed:1` 是假成功
- Task F brief 已加入 TDD + end-to-end verification 硬约束 + email.ts 的 housekeeping fix

**2026-05-27 (Session 12)** — PR #1 squash-merged to main (`46782a3`). Pre-merge cleanup: deleted `findings.md` / `progress.md` / `task_plan.md` (OAuth migration residue), added `.claude/` to `.gitignore`. Vercel env scope 系统性修复：22/22 env vars 全补到 Preview + Development scope（昨天迁移时只设了 Production）。Vercel CLI 升级 50.37.1 → 54.5.0（原版本非交互模式 bug）。卡住的 build queue（`nib9ar4st`）cancel 后 `8exmjwmiq` 重跑，Preview 绿。PR #1 admin squash merge（E2E 历史上就挂，不是本 PR 引入）。本地 main fast-forward 到 origin/main。发现独立 bug：`e2e/auth.setup.ts:52` 读 `SUPABASE_ANON_KEY`（bare 名），但 CI workflow 只注入 `NEXT_PUBLIC_SUPABASE_ANON_KEY`（前缀名）→ `Error: supabaseKey is required`。Fix = 1 行 YAML，决策 A：作为 Task E 的 Part 0 一起提交。

**2026-05-26 (Session 11 — CHECKOUT)** — Jason signs off. Roadmap approved. Steps 1a (Make.com disabled) + 1d (Resend DKIM CNAME added) marked DONE by Jason. Comprehensive checkout written. Tomorrow: check-in → dispatch Task E (Weekly Digest) immediately.

**2026-05-26 (Session 10)** — D11 executed. Jose commit `3f8d258`: synchronous AI in `/api/ingest`, apps/worker deleted, BullMQ killed. Commit `b384f51`: drop address shortened to `drop@dropnote.me`. Commit `de1dba1`: legal pages fixed (Jason Gao data controller, Google OAuth only, /aup route). Commit `001b28a`: AI error handling with retry logic + 4 unit tests. All 21 Vercel env vars had trailing `\n` — Python script cleaned them. SendGrid Inbound Parse configured (Host = `dropnote.me`, URL = `https://dropnote.me/api/ingest?key=<secret>`). Porkbun MX: deleted `MX inbound`, added `MX @ → mx.sendgrid.net prio 10`. SendGrid domain auth: 3 CNAMEs at Porkbun (em8985, s1._domainkey, s2._domainkey). Make.com disabled by Jason (1a). Resend DKIM CNAME added by Jason (1d). `jasonusca@gmail.com` has 12 items — was OAuth account mismatch, not a bug.

**2026-05-26 (Session 9)** — Jason added Google Cloud Console Redirect URI + JS Origin. Mic verified OAuth endpoint → HTTP 302, no mismatch. Task A+C brief dispatched to Jose (commit a22f418, push confirmed remote main).

**2026-05-26 (Sessions 1–8)** — Full Vercel migration (house-os deleted, drop-note imported, 22 env vars, dropnote.me domain); Supabase `.env.local` typo fixed (`rccbycfvzquncmnscoyy` → `fywrywzmksxqggvwfstn`); claude-wall paused to free slot; Supabase site_url + redirect allowlist patched via Management API; Google OAuth Client ID `1098465696948-k8m9uf2dfclskrgd152pm4pq08dhkei0` updated.

---

## Current state of play (as of 2026-05-27 EOD)

### What is live and working at dropnote.me

- **URL:** `https://dropnote.me` — Vercel, production deploy, 200 OK
- **Auth:** Google OAuth, Supabase `fywrywzmksxqggvwfstn`, site_url = `https://dropnote.me`
- **Drop address:** `drop@dropnote.me` (MX → mx.sendgrid.net → SendGrid Inbound Parse → `/api/ingest`)
- **AI pipeline:** Synchronous in `/api/ingest` (D11). OpenAI GPT-4o-mini. No worker, no BullMQ, no Railway.
- **Upstash Redis:** Still live for rate-limit + abuse counter + save-limit counters
- **Legal pages:** `/terms`, `/privacy`, `/aup` — Jason Gao data controller, Google OAuth, 50-item free tier
- **Emails:** Resend, `hello@dropnote.me`. Resend domain `dropnote.me` verified (D14). `send.dropnote.me` subdomain = outbound isolation. Digest delivery confirmed end-to-end 2026-05-27.
- **SendGrid domain auth:** 3 CNAMEs added. Verification pending (may need 24–48h DNS propagation)
- **Make.com:** DISABLED by Jason (1a done)
- **Onboarding (PR #3):** WelcomeModal + empty state polish code complete on `s8/onboarding` branch. PR #3 OPEN. CI: lint/typecheck/build/Vercel Preview all green; unit test coverage check FAIL (55.67% statements < 60%, 44.8% branches < 50%). Awaiting Jason's path decision (a/b/c) tomorrow.

### Pre-launch blockers — FINAL STATUS

| # | Blocker | Status |
|---|---|---|
| 1 | Domain (dropnote.me, DNS, MX) | **DONE** |
| 2 | AI error handling (retryable failures, status updates) | **DONE** — commit `001b28a` |
| 3 | Dockerfile HOSTNAME (`ENV HOSTNAME=0.0.0.0`) | **DONE** — commit `a22f418` (moot after D11 killed worker, but fix is in) |
| 4 | Legal pages (Jason Gao data controller, Google OAuth, free tier only) | **DONE** — commit `de1dba1` |
| 5 | Domain migration (dropnote.com → dropnote.me, all files) | **DONE** — commit `a22f418` |
| 1a | Make.com disable | **DONE** — Jason executed |
| 1d | Resend DKIM CNAME | **DONE** — Jason executed |

**All P0 pre-launch blockers are CLEARED.** The app is production-ready for launch.

### Cleanup deferred (not blocking)

| Item | Status |
|---|---|
| Old Vercel account drop-note project deletion (step 7 / 1b) | Low priority, Jason executes when convenient |
| SendGrid domain auth verification (24–48h DNS propagation) | Self-resolving |
| `jasonusca@gmail.com` end-to-end browser verify (OAuth + 12 items) | Jason can do anytime |

---

## Decisions locked

- **D1 (2026-04-02)** — Killed $9.99/$49.99 paid tiers. Launch 100% free hosted, $4.99/mo hosted tier only if 100+ active users. Source: `docs/next-plan.md` § 3.
- **D2 (2026-04-02)** — Omnivore-replacement positioning. Skip head-to-head vs Readwise. Source: `docs/next-plan.md` § 2.
- **D3 (pre-launch)** — Free limit = 50 items (raised from 20 per PM recommendation).
- **D4 (pre-launch)** — Docker registry = `ghcr.io/lego651/drop-note`.
- **D5 (pre-launch)** — Privacy data controller = Jason Gao (not LEDOO FORTUNE FINANCIAL INC.).
- **D6 (2026-04-27)** — SUPERSEDED by D10. Launch was paused; now actively pushing.
- **D7 (2026-04-02)** — Stripe code stays in repo, null-checked SDK init. Reactivation = 1-PR when 100 active users threshold hits.
- **D8 (pre-2026-04-02)** — AI = OpenAI GPT-4o-mini. Only LEDOO product not on Anthropic. Self-host configurable via `.env`.
- **D9 (2026-05-25)** — Domain = `dropnote.me` (Porkbun, USD $5/yr). `dropnote.so` rejected ($65/yr). `dropnote.com` deferred: trigger = 100 active users → GoDaddy Broker + $4.99 hosted tier.
- **D10 (2026-05-25)** — house-os shutdown. drop-note = #1 product (trading = #0 execution, not product). D6 (launch pause) is superseded. Actively pushing to launch.
- **D14 (2026-05-27)** — Resend domain `dropnote.me` officially configured via Resend API + DNS. Records: TXT `resend._domainkey.dropnote.me` (DKIM), MX `send.dropnote.me → feedback-smtp.us-east-1.amazonses.com`, TXT `send.dropnote.me` (SPF). Status: Verified. Pattern locked: `send.dropnote.me` subdomain = outbound mail isolation; apex MX stays SendGrid (inbound only). Email delivery confirmed end-to-end (digest received by Jason 2026-05-27).
- **D11 (2026-05-26)** — Synchronous AI in `/api/ingest`. Kill Railway + BullMQ + `apps/worker/`. Upstash Redis retained (rate-limit / abuse / save-limit counters). Reasoning: (1) AI processing 3–10s, well under Vercel 60s default; (2) saves $5/mo Railway, eliminates 2-month dead worker; (3) architecture simplification: SendGrid → /api/ingest (insert + AI + update) → done; (4) SendGrid auto-retry 72hr on 5xx. **Monitoring requirement:** every `/api/ingest` request MUST log total handling time + AI time (Sentry breadcrumb / structured log) for p50/p95 tracking. **Re-evaluate triggers (any one → migrate to QStash async):** (a) active users ≥ 100; (b) /api/ingest p95 approaches 30s; (c) Vercel function timeout errors appear in logs. Jason quote: "等我们有100个用户，并且要monitor processing time, 如果 server handling time 上来的话还是异步 QStash." Commit: `3f8d258`. Source: 2026-05-26 conversation.

---

## Process rules — LOCKED after Task E audit (2026-05-27)

Jose 的每个 PR 必须满足以下三条，否则 Mic 拒绝接受：

1. **TDD 硬约束** — 所有新 route / 新 lib function 必须先写 `*.test.ts`，见红后写实现，见绿后 commit。PR 里必须有对应测试文件，否则视为不完整。
2. **End-to-end verification 硬约束** — 交工时必须附证据。API route 要 curl 截图（200 + 正确响应体）。UI 变更要 Playwright 截图或浏览器实际截图。Email 功能要 Resend Activity 截图（邮件 delivered 状态）。声称"lint/typecheck/build green"不等于 verification done。
3. **禁止 try/catch 吞错** — 任何新写的 async 函数不能用 `try { ... } catch (e) { console.error(e) }` 模式把错误吞掉。必须让错误向上抛，由调用方（cron handler、route handler）用 `Promise.allSettled` 正确计数 failed，让监控能看到真实失败率。

**背景：** Task E PR #2 Jose 跳过了 TDD，跳过了 email 送达验证。`lib/email.ts` 的 try/catch 导致 Resend 403 被吞，cron 返回 `processed:1` 是假成功，Jason 没收到任何 digest 邮件——直到手动挖 Resend logs 才发现。这三条规矩是为了防止这类"代码绿、产品坏"的情况。

**典范行为（2026-05-27 Task F）：** Jose 在 Task F 中遇到 Google OAuth only 无法做 headless 自动化时，**主动 block 并 ping Mic 请求 manual verify，而不是伪造假截图**。这是新流程第一次真正落地的证据。结果：Jason 本机 manual verify 成功，截图存档，PR #3 有真实的 browser evidence。这个行为模式要保持：证据必须真实，宁可 block 也不伪造。

---

## Approved roadmap (Jason approved 2026-05-26 EOD)

Tasks are ordered. All P0 blockers done. Next = Task E.

| Task | Description | Status |
|---|---|---|
| A | Domain migration (dropnote.com → dropnote.me) | DONE — `a22f418` |
| B | AI error handling + retry logic | DONE — `001b28a` |
| C | Dockerfile HOSTNAME | DONE — `a22f418` |
| D | Legal pages (Jason Gao, Google OAuth, free tier) | DONE — `de1dba1` |
| D11 | Kill Railway/BullMQ, sync AI in /api/ingest | DONE — `3f8d258` |
| Drop address | Shorten to drop@dropnote.me | DONE — `b384f51` |
| **E** | **Weekly Digest (Vercel Cron + Resend)** | **DONE — PR #2 merged `36e228e`** |
| **F** | **Onboarding (welcome modal + empty state polish)** | **PR #3 OPEN — 覆盖率门槛失败，待拍板路径 a/b/c** |
| G | SEO blog post ("Open-Source Omnivore Alternative") | After F |
| H | Email deliverability test (Gmail/Outlook/ProtonMail) | After F |
| I | Stripe reactivation ($4.99/mo hosted tier) | ONLY at 100 active users |

---

## On resume (next session)

**PR #3 (Task F) is OPEN. 覆盖率门槛挂，明天第一件事是拍板。**

On resume:
1. Read this `.mic-state.md`.
2. Run `gh pr checks 3 --repo lego651/drop-note` 确认 CI 当前状态。
3. 向 Jason 呈报覆盖率路径选项，让他拍板：
   - **(a) 补测试** — 派 Jose 为 WelcomeModal + empty state 补单元测试，把 statements 拉到 ≥60%，branches 拉到 ≥50%。干净，无 tech debt，但需要 Jose 再一轮。
   - **(b) 临时降门槛** — 把 `vitest.config.ts` 里 `statements: 60` 降到 `55`，`branches: 50` 降到 `44`，list tech debt ticket，merge PR #3。快，但留下覆盖率债。
   - **(c) Admin-merge PR #3** — 跳过门槛直接 merge，开独立 follow-up ticket 补测试。最快，tech debt 最明显。
4. 拍板后，由 Mic 执行对应动作（a → brief Jose；b/c → Mic 自己改 config 或 admin merge）。
5. **PR #3 merge 后**：dispatch Task G（SEO blog post "Open-Source Omnivore Alternative"）— brief 在下方 pre-staged。
6. Task G 后：dispatch Task H（email deliverability test）。

**Task G brief 状态：pre-staged，等 PR #3 merge 后 dispatch。不要提前动。**

---

## Pre-staged Jose Brief — Task F (Onboarding)

> Copy this verbatim when dispatching to `techlead-jose`. Branch, commit prefix, acceptance criteria all pre-decided.
> TDD + verification + no-swallow-errors are MANDATORY in this brief — not optional.

---

**Brief: Task F — Onboarding Polish (Welcome Modal + Empty State + email.ts housekeeping)**

**Repo:** `lego651/drop-note` (monorepo, pnpm workspaces)
**Base branch:** `main`
**New branch:** `s8/onboarding`
**Commit prefix:** `[s8]`

---

**Context:**

drop-note is an email-to-dashboard content saver (AGPL-3.0). Live at `https://dropnote.me`. Users log in with Google OAuth → land on `/items`. If they have no items, they see an inline empty state (text + copy button). There is also a `OnboardingPanel` component (`components/dashboard/onboarding-panel.tsx`) that is built but never rendered anywhere.

The problem: new users land on an empty dashboard with minimal guidance. They don't know their drop address without scrolling or looking around. There is no first-time welcome experience. We need to fix this without adding complexity.

**Architecture note (D11):** synchronous AI in `/api/ingest`. No BullMQ, no worker. No queue layer needed for this task.

---

**MANDATORY PROCESS RULES (non-negotiable for this PR):**

1. **TDD:** Write the test file FIRST. See it fail (or confirm it tests what you think). THEN write the implementation. THEN see it pass. Every new component and lib function must have a corresponding `*.test.ts` or `*.test.tsx`.
2. **Verification evidence:** When you submit the PR, attach: (a) screenshot of the welcome modal rendering in a browser, (b) screenshot of the improved empty state, (c) confirmation that `pnpm test` passes with the new test files included.
3. **No error swallowing:** Do not use `try { ... } catch (e) { console.error(e) }` to silently eat errors. If something fails, throw — let the caller handle it.

---

**What to build:**

**1. Fix `lib/email.ts` — stop swallowing Resend errors (housekeeping, ~5 lines)**

File: `apps/web/lib/email.ts`

Current pattern (BAD):
```typescript
try {
  await resend.emails.send({ ... })
} catch (err) {
  // Never throw — don't break cron flow
  console.error('[email] Failed to send weekly digest email:', err)
}
```

This hides real failures. Resend 403 errors were silently swallowed in Task E, causing false `processed:N` counts in the cron.

Fix: remove the try/catch from `sendWeeklyDigestEmail` and `sendWelcomeEmail`. Let errors throw. The callers (`/api/cron/digest` and the auth callback) must handle errors:
- `/api/cron/digest`: already uses `Promise.allSettled` — it will correctly count `rejected` results as failed
- Auth callback (welcome email): wrap only there with a non-fatal catch, logging the error but not blocking sign-in

After this fix, if Resend is misconfigured or returns a 4xx, the cron's `failed` count will be non-zero and visible in logs.

Write a test: `apps/web/lib/__tests__/email.test.ts` — mock Resend, verify that when Resend throws, `sendWeeklyDigestEmail` propagates the error (does not swallow it).

**2. Welcome modal for first-time users**

File: `apps/web/components/dashboard/WelcomeModal.tsx` (new)

Trigger: show once per account, on first dashboard load after sign-up. Use `localStorage` key `drop-note:welcomed` — if absent, show modal. On dismiss, set the key.

Modal content (use shadcn `Dialog` — already in project at `components/ui/dialog.tsx`):
- Title: "Welcome to drop-note"
- Body: 3 steps laid out visually (same design as OnboardingPanel's steps):
  1. "Email anything to your drop address"
  2. "AI summarizes and tags it"
  3. "Find it in your dashboard"
- Drop address display: `drop@dropnote.me` in a monospace box with a copy button (same pattern as `OnboardingPanel`)
- Primary CTA: "Send yourself a test email" (mailto link)
- Secondary: "Got it" (dismiss button)
- The modal must be dismissible with Escape key and clicking outside

Where to render: add `<WelcomeModal />` inside `ItemsPageClient` (already a client component). It renders null if `localStorage` key is set; shows modal on first visit.

Write a test: `apps/web/components/dashboard/__tests__/WelcomeModal.test.tsx` — test that (a) modal renders when localStorage key is absent, (b) modal does NOT render when key is present, (c) dismiss sets the key.

**3. Improve empty state in `ItemsPageClient`**

File: `apps/web/components/items/ItemsPageClient.tsx`

Current empty state (lines 281–302): minimal text + copy button.

Improve to match the `OnboardingPanel` visual style:
- Show the 3-step flow (Email it → AI tags it → Find it here) as small numbered pills
- Show the drop address in a monospace box with copy button (same as OnboardingPanel and WelcomeModal)
- Add a mailto CTA button: "Send yourself a test email"
- Keep it compact — this is inline in the items list, not a full-page panel

Do NOT delete `OnboardingPanel` — just make the inline empty state better. `OnboardingPanel` can stay as-is (may be used in future contexts).

Write a test: update `apps/web/components/__tests__/ItemCard.test.tsx` or add a new test file for the empty state — verify that when `items=[]` and not in search mode, the empty state renders with the drop address visible.

---

**What NOT to touch:**

- Do NOT re-add BullMQ, Redis queue, or apps/worker
- Do NOT change `/api/ingest` behavior
- Do NOT touch Stripe code
- Do NOT change auth flow or Google OAuth setup
- Do NOT delete `OnboardingPanel`
- Do NOT add any new external service or npm package without confirming it's not already in the project

---

**Acceptance criteria:**

1. `pnpm turbo lint && pnpm turbo typecheck && pnpm --filter @drop-note/web build` passes with zero errors
2. `pnpm test` passes — new test files for `email.test.ts`, `WelcomeModal.test.tsx`, and empty-state coverage all green
3. `sendWeeklyDigestEmail` and `sendWelcomeEmail` in `lib/email.ts` throw on Resend error (no silent catch). Test confirms this.
4. Welcome modal renders on first visit (localStorage key absent) — browser screenshot attached as evidence
5. Welcome modal does NOT render on subsequent visits (localStorage key present) — confirmed in test
6. Empty state in `/items` shows 3-step flow + drop address + mailto CTA — browser screenshot attached
7. Commit(s) prefixed `[s8]`, subject under 72 chars, co-author trailer:
   ```
   Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
   ```

---

**Constraints:**

- shadcn `Dialog` already in project (`components/ui/dialog.tsx`) — do NOT run `npx shadcn add` unless you confirm it's missing
- Use semantic Tailwind tokens (`bg-background`, `text-muted-foreground`) — never raw color classes
- TypeScript strict mode — no `as any` except in test files
- Server Components by default — `WelcomeModal` needs `'use client'` (uses localStorage + state); `ItemsPageClient` is already client

---

## Files created / modified this session (2026-05-27, Sessions 12–15 — FULL DAY)

| File | Action | Note |
|---|---|---|
| `apps/web/lib/email.ts` | Modified | `113fec8` — remove try/catch swallowing, let errors throw |
| `apps/web/lib/__tests__/email.test.ts` | Created | `113fec8` — test: sendWeeklyDigestEmail propagates Resend errors |
| `apps/web/components/dashboard/WelcomeModal.tsx` | Created | `d68d4f9` — first-visit modal, localStorage key, shadcn Dialog |
| `apps/web/components/dashboard/__tests__/WelcomeModal.test.tsx` | Created | `d68d4f9` — test: renders when key absent, skips when present |
| `apps/web/components/items/ItemsPageClient.tsx` | Modified | `762c2e7` — upgraded empty state: 3-step pills + drop address CTA |
| `docs/verification/task-f/welcome-modal.png` | Created | `8a69301` — Jason's manual verify screenshot |
| `docs/verification/task-f/empty-state.png` | Created | `8a69301` — Jason's manual verify screenshot |
| `docs/.mic-state.md` | Modified | Full 2026-05-27 EOD checkout (Sessions 12–15) |

## Files created / modified this session (2026-05-27, Sessions 12–13)

| File | Action | Note |
|---|---|---|
| `findings.md` / `progress.md` / `task_plan.md` | DELETED | OAuth migration residue cleanup before PR #1 merge |
| `.gitignore` | Modified | Added `.claude/` entry |
| `.github/workflows/ci.yml` | Modified | Part 0: added `SUPABASE_ANON_KEY` to e2e job env — commit `68cad4f` |
| `supabase/migrations/20260527000001_add_digest_enabled.sql` | Created | digest_enabled column — needs `db push` by Jason |
| `packages/shared/src/database.types.ts` | Modified | digest_enabled added manually to Row/Insert/Update |
| `apps/web/app/api/cron/digest/route.ts` | Created | Weekly digest cron handler |
| `apps/web/vercel.json` | Created | Both cron jobs (purge-trash + digest) |
| `apps/web/lib/email.ts` | Modified | Added `sendWeeklyDigestEmail` function |
| `apps/web/components/ui/switch.tsx` | Created | Minimal Switch component (no Radix dep) |
| `apps/web/app/(dashboard)/settings/SettingsClient.tsx` | Modified | Weekly Digest toggle section |
| `apps/web/app/(dashboard)/settings/page.tsx` | Modified | Queries + passes `digest_enabled` |
| `docs/.mic-state.md` | Modified | This file — Session 13 checkpoint |

---

## Files created / modified (2026-05-26, all sessions combined)

| File | Action | Note |
|---|---|---|
| `apps/web/` (14 files) | Modified | commit `a22f418` — dropnote.com → dropnote.me + worker HOSTNAME |
| `apps/worker/` | DELETED | commit `3f8d258` — D11, kill worker |
| `apps/web/app/api/ingest/route.ts` | Modified | commits `001b28a`, `3f8d258` — sync AI + retry logic |
| `apps/web/app/privacy/page.tsx` | Modified | commit `de1dba1` — Jason Gao, Google OAuth |
| `apps/web/app/terms/page.tsx` | Modified | commit `de1dba1` — Google OAuth, free tier only |
| `apps/web/app/aup/page.tsx` | Created | commit `de1dba1` — new /aup route |
| `docs/relaunch-day0.md` | Created | Day 0 execution package |
| `docs/jose-brief-relaunch.md` | Created | Full Jose brief (Tasks A–E) |
| `docs/.mic-state.md` | Modified | This file — comprehensive checkout |

---

## Historical notes (kept for context, not for action)

- Sprints 0–6 all shipped. App ~85–90% production-ready per `docs/next-plan.md` (2026-04-02 snapshot).
- Original launch plan: Apr 19-20 weekend. Paused 2026-04-27. D10 (2026-05-25) superseded D6 — now actively pushing.
- Repo: `lego651/drop-note`. License: AGPL-3.0.
- Commits today: `a22f418`, `001b28a`, `de1dba1`, `b384f51`, `3f8d258` — all pushed to main.
- Old Vercel account still has a dead drop-note project (cleanup deferred, not blocking).
- `jasonusca@gmail.com` is the correct account for testing (has 12 items). `jasongaoxiaofeng@gmail.com` is a second Google account that has no items.
